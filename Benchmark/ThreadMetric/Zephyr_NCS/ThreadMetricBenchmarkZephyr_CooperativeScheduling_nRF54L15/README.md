# ThreadMetricBenchmarkZephyr_CooperativeScheduling_nRF54L15

Standalone NCS/Zephyr wrapper for the official Zephyr Thread-Metric scenario `benchmark.thread_metric.cooperative` on `nrf54l15dk/nrf54l15/cpuapp`.

Build from an NCS workspace root or anywhere with `west` and `ZEPHYR_BASE` available:

```bash
west build -p always -b nrf54l15dk/nrf54l15/cpuapp ThreadMetricBenchmarkZephyr_CooperativeScheduling_nRF54L15
west flash
```
